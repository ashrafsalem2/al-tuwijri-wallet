Drop your Lottie animation here as:  login.json

Where to get one (free):  https://lottiefiles.com  (search "login" / "security")
The login screen loads assets/lottie/login.json automatically.
Until then, it shows a styled fallback icon — no crash.
